// Apply this change to disable mock data
const USE_MOCK_DATA = false; // Force real backend usage

// Update BASE_URL to use env config
import { env } from "../../config/env.config";
const BASE_URL = env.API.URL;
